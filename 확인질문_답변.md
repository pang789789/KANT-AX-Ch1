# AX 2회차 Ch 1 확인 질문

아래는 작성된 코드에 대한 답변입니다. 제출 전에 코드를 읽고 본인이 이해한 표현으로 확인해주세요.

## Python

1. **for 반복문은 이 프로그램에서 어떤 역할을 하나요?**
   점수 리스트의 원소를 하나씩 확인하여 총점과 조건에 맞는 개수를 계산합니다. 단어 분석에서는 문장을 나눈 단어를 하나씩 확인해 딕셔너리의 횟수를 갱신하고, 결과를 출력합니다.

2. **딕셔너리의 Key와 Value에는 각각 무엇이 저장되나요?**
   Key에는 단어 문자열, Value에는 그 단어의 등장 횟수인 정수가 저장됩니다. 예를 들어 `{'공부는': 2}`입니다.

3. **같은 단어가 다시 등장하면 딕셔너리 값은 어떻게 달라지나요?**
   기존 값에 1을 더합니다. 처음 등장한 단어는 1로 저장하고, 두 번째 등장하면 2로 바뀝니다.

4. **함수를 사용하는 이유는 무엇인가요?**
   반복되는 로직을 한곳에 모아 재사용하고 수정하기 쉽게 하기 위해서입니다. `count_words(sentence)`는 문장을 받아 빈도 딕셔너리를 반환하므로 다른 문장에도 같은 함수를 사용할 수 있습니다.

5. **AI를 사용했다면 AI가 제안한 코드에서 직접 확인하거나 수정한 부분은 무엇인가요?**
   이 결과물은 AI가 코드 작성과 실행 검증을 도왔습니다. AI가 확인한 항목은 `split()` 기준에서 ‘파이썬’ 3회와 ‘파이썬을’ 1회가 구분되는 점, 함수 결과와 직접 작성한 반복문의 결과가 같은 점, 잘못된 입력의 예외처리입니다. 이는 AI의 수행 내역이며, 제출자는 직접 실행하거나 확인한 항목을 본인의 실제 경험에 맞게 덧붙여야 합니다.

## SQL & 데이터베이스

1. **PRIMARY KEY는 왜 필요한가요?**
   각 행을 중복 없이 식별하기 위해 필요합니다. 이 테이블에서는 `member_id`가 회원을 구분합니다. 같은 이름의 회원이 있어도 번호로 정확히 선택할 수 있습니다. 기본 키는 중복과 NULL을 허용하지 않습니다.

2. **WHERE 없이 UPDATE 또는 DELETE를 실행하면 어떤 문제가 발생할 수 있나요?**
   `UPDATE`는 모든 행을 수정하고, `DELETE`는 모든 행을 삭제할 수 있습니다. 그래서 먼저 같은 WHERE 조건으로 SELECT를 실행해 대상을 확인합니다.

3. **SELECT *와 필요한 컬럼만 선택하는 SQL의 차이는 무엇인가요?**
   `SELECT *`는 모든 컬럼을 가져옵니다. `SELECT name, email`은 이름과 이메일만 가져와 결과를 읽기 쉽고 불필요한 데이터를 줄일 수 있습니다.

4. **COUNT()와 AVG()는 각각 어떤 값을 계산하나요?**
   `COUNT(*)`는 행 수를 셉니다. `COUNT(컬럼)`은 해당 컬럼이 NULL이 아닌 행 수를 셉니다. `AVG(age)`는 NULL을 제외한 나이의 평균을 계산합니다. 이번 최종 결과는 4명, 평균 28.75세입니다.

5. **Python에서 데이터를 처리하는 것과 DB에서 SQL로 데이터를 조회하는 것의 차이는 무엇인가요?**
   Python에서는 리스트와 딕셔너리 같은 자료구조를 반복문과 함수로 직접 처리합니다. SQL에서는 DB에 저장된 테이블을 대상으로 필요한 조건과 집계를 선언하면 DB가 처리합니다. Python은 다양한 프로그램 로직에, SQL은 저장된 데이터의 조회와 관리에 활용할 수 있습니다.
