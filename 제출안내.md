# AX 2회차 Ch 1 과제

## 포함 파일

| 파일 | 내용 |
|---|---|
| python_assignment.ipynb | Python 필수 Part 1~4, 도전 문제 4종 및 실행 출력 |
| python_execution.txt | Python 실제 실행 출력 |
| word_frequency.txt | Python에서 저장한 단어별 빈도 |
| members_assignment.sql | Schema, Table, CRUD, 집계 및 SQL 도전 문제 5종 |
| sql_execution.txt | PostgreSQL에서 전체 SQL을 순서대로 실행한 쿼리와 결과 |
| 스크린샷 2026-09-07 200146.png | DBeaver SQL 실행 결과 화면 |
| 확인질문_답변.md | Python 5개, SQL 5개 확인 질문 답변 초안 |
| 제출안내.md | 실행 방법, 검증 내용, 제출 안내 |

## 실행 방법

### Python

Jupyter에서 `python_assignment.ipynb`를 열고 코드 셀을 실행합니다. 외부 패키지 설치는 필요하지 않습니다. `word_frequency.txt`는 노트북과 같은 폴더에 생성되며 다시 실행하면 갱신됩니다.

### PostgreSQL + DBeaver

1. DBeaver에서 본인의 PostgreSQL 실습 DB에 연결합니다.
2. 빈 실습 DB에서 `members_assignment.sql`을 UTF-8로 엽니다.
3. 파일 전체를 SQL 스크립트 실행으로 위에서부터 실행합니다.
4. 데이터베이스 탐색기를 새로고침하여 `practice` 스키마와 `members` 테이블, 5개 컬럼을 확인합니다.
5. 결과 탭에서 조회·변경·집계 결과를 확인합니다. 수업에서 화면 캡처를 별도로 요구하면 해당 결과를 캡처합니다.

SQL 파일은 하나의 트랜잭션으로 실행하고 마지막에 COMMIT합니다. 기존 `practice.members`가 있으면 테이블 생성 단계에서 오류가 발생합니다. 재실행할 때는 새 실습 DB를 사용하세요. 오류 후 트랜잭션이 중단된 연결은 ROLLBACK으로 종료한 뒤 진행합니다. 이 파일은 기존 테이블이나 데이터를 초기화하지 않습니다.

## 실제 검증 결과

2026-09-07에 Python 3 및 PostgreSQL 18.4에서 실행했습니다. SQL은 로컬 실습 DB에서 실행했고 DBeaver 결과 화면을 캡처했습니다. 실행 결과는 실제 출력값을 기준으로 정리했습니다.

| 검증 항목 | 결과 |
|---|---|
| Python 80점 이상 | 90, 88, 100, 80 / 4개 |
| Python 총점 / 평균 | 555 / 79.29 |
| 단어 수 / 서로 다른 단어 수 | 16 / 12 |
| 2회 이상 단어 | 파이썬 3회, 공부는 2회, 재미있다 2회 |
| 함수 결과 비교 / 빈 문자열 | 통과 / 빈 딕셔너리 |
| SQL 최초 등록 | 5명 |
| 수정 / 삭제 | 1번 회원 25→30세 / 5번 회원 삭제 |
| 최종 회원 수 / 평균 나이 | 4명 / 28.75세 |
| 최고 / 최저 나이 | 35세 / 22세 |
| 25세 이상 회원 | 3명 |
| 연령대 집계 | 20대 2명, 30대 2명 |
| 최근 가입 회원 | 최유나 |
| 평균 초과 회원 | 박서준, 김민수 |

도전 문제와 집계는 수정·삭제 후 남은 4명 기준입니다. 모든 회원 정보는 가상 데이터입니다.

## 요구사항 해석과 오류 확인

- Python Part 1은 구체적인 데이터와 조건이 없어 점수 리스트와 80점 이상 조건을 선택했습니다.
- 빈도 출력 기준이 지정되지 않아 2회 이상으로 정했습니다.
- 원문의 ‘파이썬 4회’ 예시는 지정된 `split()` 방식과 차이가 있습니다. 조사를 제거하지 않으므로 ‘파이썬’은 3회, ‘파이썬을’은 1회로 처리했습니다.
- `count_words(123)`은 문자열이 아니므로 TypeError를 발생시키고 처리합니다. 잘못된 입력 자료형을 확인하는 의도적 검증 사례입니다.
- `int('두 번')`은 정수 변환이 불가능하여 ValueError가 발생합니다. 예외를 잡아 안내를 출력한 후 유효한 기준 2로 다시 실행합니다.
- 실제 실행 시 처리되지 않은 Python 오류나 SQL 오류는 없었습니다. 위의 의도적 예외를 개발 중 우연히 겪은 오류로 서술하지 않았습니다.

## 제출 전

압축을 풀어 코드를 실행하고, 확인 질문 답변을 본인의 이해와 실제 수행 내용에 맞게 확인해주세요. GitHub 저장소 링크는 선택 사항이므로 포함하지 않았습니다.

백오피스에 파일별로 첨부하거나, ZIP을 허용하는 경우 제공된 ZIP을 첨부할 수 있습니다. 과제 제출 사이트로 업로드하거나 제출을 완료한 상태는 아닙니다.

원문: https://teamsparta.notion.site/AX_2-Ch-1-3ce2dc3ef51480d1ab73e7e01f3a024
